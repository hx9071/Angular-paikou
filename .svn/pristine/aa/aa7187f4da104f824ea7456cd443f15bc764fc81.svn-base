import { CUSTOM_ELEMENTS_SCHEMA } from '@angular/core';
import { async, ComponentFixture, TestBed } from '@angular/core/testing';
import { PutlatAddPage } from './putlat-add.page';

describe('PutlatAddPage', () => {
  let component: PutlatAddPage;
  let fixture: ComponentFixture<PutlatAddPage>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ PutlatAddPage ],
      schemas: [CUSTOM_ELEMENTS_SCHEMA],
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(PutlatAddPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
