import { CUSTOM_ELEMENTS_SCHEMA } from '@angular/core';
import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { PutlatMapPage } from './putlat-map.page';

describe('PutlatMapPage', () => {
  let component: PutlatMapPage;
  let fixture: ComponentFixture<PutlatMapPage>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ PutlatMapPage ],
      schemas: [CUSTOM_ELEMENTS_SCHEMA],
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(PutlatMapPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
